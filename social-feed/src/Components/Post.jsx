const Post = (props) => {
    
    return (
        <div class ="div">
            <h3 class = "name">{props.name}</h3>
            <p class="post">{props.post}</p>
            

        </div>
    );
        
}

export default Post;
